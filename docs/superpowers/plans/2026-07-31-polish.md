# v0.10.9 打磨 — Implementation Plan

> 按文件组织，11 个 issue 分为 5 个 task

---

### Task 1: 时间输入打磨（#1, #2, #5）

**Files:**
- `frontend/src/components/EditModal.vue` — `parseSignedTime` + `autocomplete="off"`
- `frontend/src/components/CutConfirm.vue` — `parseTime` + `autocomplete="off"`
- `frontend/src/components/ExportImagePanel.vue` — 时间范围 `autocomplete="off"`
- `frontend/src/components/Timeline.vue` — halfline 减量

**#1**: `parseSignedTime` / `parseTime` 新增 `HHMM`（无冒号）分支
**#2**: 所有时间 `<input>` 加 `autocomplete="off"`
**#5**: `allLabels` 渲染 halfline 时跳过最后一个

---

### Task 2: 穿界块 split（#4）

**Files:**
- `frontend/src/components/EditModal.vue` — `save()` 检测越界 + split + 弹 confirm
- `frontend/src/store/timelog.js` — 辅助：`splitAndSave(sourceDate, localStart, localEnd, cutMeta)`

**逻辑**：
1. `save()` 中用 `localToStorage` 得到 s/en 后，计算实际显示范围
2. 若 `local.start < todayRange.start`（穿 toPrev 边）→ confirm → split 前半到昨天
3. 若 `local.end > todayRange.end`（穿 toNext 边）→ confirm → split 后半到明天
4. 同 ID 两块分别写入各自的 localStorage key

---

### Task 3: 统计 + 标签（#6, #10, #11）

**Files:**
- `frontend/src/utils/stats.js` — `computeCardsData` 新增 gap 计算
- `frontend/src/components/SettingsPanel.vue` — 标签排序按钮 + 拖拽 + hex 输入

**#6**: card 新增 `countGaps` 字段；`computeCardsData` 扫描空白分钟
**#10**: 标签列表排序按钮（按分组字母）；拖拽调序（HTML5 drag/drop）
**#11**: 颜色行加 `#RRGGBB` 文本框

---

### Task 4: T 键 + 红线 + 拖拽（#7, #9, #12）

**Files:**
- `frontend/src/App.vue` — T 键 end 跨帧修复
- `frontend/src/components/Timeline.vue` — `nowLineY` 优先级 + 拖拽检测

**#7**: end 用统一坐标 (`toDisplayBlock` 换算出 now 的 unified minute，与 s 比较)
**#9**: `onBlockMouseDown` 非 edge 分支加 `onDayMouseDown(e)` 启动创建拖拽
**#12**: `nowLineY()` today 区段优先于 glue-prev

---

### Task 5: 胶水区显示修复（#13）

**Files:**
- `frontend/src/components/Timeline.vue` — 诊断 + 修复

**排查路径**：`toDisplayBlock` → `blockTop` → `fmtSigned` 链
